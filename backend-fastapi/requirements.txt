# ─────────────────────────────────────────────
# backend-fastapi/requirements.txt
# AI Learner Lab — Control Plane API
# ─────────────────────────────────────────────

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1          # ASGI server (includes websockets + uvloop)

# --- Async Database ---
sqlalchemy[asyncio]==2.0.36        # Async ORM
asyncpg==0.30.0                    # Async PostgreSQL driver (used with SQLAlchemy)
alembic==1.14.0                    # DB migration tool

# --- Data Validation ---
pydantic==2.10.3                   # Pydantic v2 (used by FastAPI schemas)
pydantic-settings==2.6.1           # Settings / .env loader

# --- ML Job Orchestration ---
skypilot[kubernetes,aws]==0.7.0    # SkyPilot SDK — K3s (Kubernetes) + AWS Spot

# --- HTTP / SSE ---
httpx==0.27.2                      # Async HTTP client (health checks, internal calls)
sse-starlette==2.1.3               # Server-Sent Events for /api/logs streaming

# --- Auth / Security ---
python-jose[cryptography]==3.3.0   # JWT handling (used with oauth2-proxy headers)
passlib[bcrypt]==1.7.4             # Password hashing utility

# --- Utilities ---
python-dotenv==1.0.1               # Load .env locally (not used in prod — K8s injects vars)
python-multipart==0.0.12           # Required for FastAPI form/file uploads
tenacity==9.0.0                    # Retry logic for SkyPilot SDK calls
